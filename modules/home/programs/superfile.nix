{ ... }:

{
  config.programs.superfile = {
    hotkeys = {
      quit = [ "q" ]; # Remove esc shortcut to avoid quit accidentally
    };

    settings = {
      transparent_background = true;
    };
  };
}
